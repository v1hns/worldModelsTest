torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=10.0.0
huggingface_hub>=0.20.0
opencv-python>=4.8.0
matplotlib>=3.7.0
scipy>=1.10.0
tqdm>=4.65.0

# VGGT (install from source)
# git clone https://github.com/facebookresearch/vggt && pip install -e ./vggt

# Optional integrations
# faiss-cpu           # retrieval acceleration
# gtsam               # nonlinear pose graph optimization
# open3d              # real-time 3D visualization
# GroundingDINO       # open-set object detection (install from source)
# rclpy               # ROS2 integration (installed via ROS2 distribution)
